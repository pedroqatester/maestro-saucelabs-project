appId: com.saucelabs.mydemoapp.rn
name: Flow - Login Locked User
tags:
  - login
  - negative
---
- launchApp:
    clearState: true
- extendedWaitUntil:
    visible: "Products"
    timeout: 50000
- runFlow:
    file: ../../../commands/login.yaml
    env:
      MAESTRO_USERNAME: "${MAESTRO_LOCKED_USERNAME}"
      MAESTRO_EMAIL_DOMAIN: "${MAESTRO_EMAIL_DOMAIN}"
      MAESTRO_PASSWORD: "${MAESTRO_LOCKED_PASSWORD}"
- assertVisible: "Sorry, this user has been locked out."